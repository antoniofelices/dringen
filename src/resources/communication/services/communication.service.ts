import { medplum, authenticateMedplum } from '@shared/fhir/medplum'
import { logger } from '@shared/utils/Logger'

export const getListCommunications = async () => {
    try {
        return
    } catch (error) {
        throw error
    }
}

export const getSingleCommunication = async () => {
    try {
        return
    } catch (error) {
        throw error
    }
}
