export type CommunicationType = {
    id: string
    title: string
    content: string
    startDate: string
    endDate: string
    location: string
    sent: string
}

export type CommunicationPayloadType = {
    title: string
    content: string
    startDate: string
    endDate: string
    location: string
}
